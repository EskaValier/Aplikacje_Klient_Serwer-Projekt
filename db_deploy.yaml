apiVersion: app/v1
kind: Deployment
metadata:
  labels:
    app: mysql-db-deploy
  name: mysql-db-deploy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql-db-deploy
  template:
    metadata:
      labels:
        app: mysql-db-deploy
    spec:
      containers:
      - name: mysql-db-deploy
        image: image-registry.openshift-image-registry.svc:5000/labproj19/mysql:14.1
        imagePullPolicy: "IfNotPresent"
        ports:
        - containerPort: 3306
        volumeMounts:
        - mountPath: /
          name: mysql-db
        resources: {}
    volumes:
    - name: mysql-db
      persistentVolumeClaim:
        claimName: mysql-db_pvc
status: {}